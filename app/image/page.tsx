import { CategoryPage } from "@/components/CategoryPage";
export const metadata = { title: "Image Tools Online — Free & Instant | tinkrkit.dev" };
export default function ImagePage() {
  return <CategoryPage category="image" description="Free browser-based image tools: compress, convert, and resize images without uploading to any server." />;
}
